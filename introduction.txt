**Node Version Manager
      ---unstall node.js(নইলে কনফ্লিক্ট হইবে)
      tsc --init for tsconfig.json
      uncomment
       // File Layout
    "rootDir": "./src",
    "outDir": "./dist",
    then create src folder
    run a file then boom